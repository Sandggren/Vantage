"""Placeholder page — aggregated planner dashboard (not yet built)."""

import streamlit as st

st.set_page_config(page_title="Planner Dashboard", page_icon="📊", layout="wide")

st.title("📊 Planner Dashboard")
st.info(
    "Coming next: aggregated trends across all reported near-misses — "
    "filterable by road-user pair, time window, and severity, with a "
    "heatmap layer and exportable summary. Not yet built.",
    icon="🚧",
)
